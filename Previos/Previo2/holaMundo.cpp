// Librería utilizada para manejar objetos de salida y entrada como cout
#include <iostream>

// Función principal del código
int main () {
    // std::cout es un objeto de salida estándar para imprimir en consola.
    // << es un operador de inserción, envía el mensaje a std::cout.
    std::cout << "Hola Mundos, estudiantes de IE0217!\n";
    // Retorna 0 porque la función main empieza con enter, 
    // además si retorna 0 indica que el código finalizo correctamente
    return 0; 
}