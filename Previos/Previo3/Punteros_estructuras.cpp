#include <iostream>
using namespace std; 

struct temp {
    // Declarando i
    int i; 
    // Declarando f
    float f;
};

int main() {
    // Declaramos el puntero de la estructura struct
    temp *ptr;
    return 0;
}