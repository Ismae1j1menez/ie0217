#include <wx/wx.h>

// Esta es la declaración de la clase MainFrame que hereda de wxFrame.
class MainFrame : public wxFrame {
public:
    // Constructor de la clase. Toma un título como argumento.
    MainFrame(const wxString& title);
};